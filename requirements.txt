# TalkForge — Python dependencies
# These versions are tested for compatibility across Python 3.10 – 3.12
# and the current Google Colab environment.
#
# Install with:  pip install -r requirements.txt
#
# Note: torch / torchvision / torchaudio are installed separately in the Colab
# notebook (CUDA wheel).  Listed here for reference in local installs.

# ── Web UI ─────────────────────────────────────────────────────────────────
gradio>=4.26.0,<5.0.0

# ── Deep learning ──────────────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# ── Model serialisation ─────────────────────────────────────────────────────
safetensors>=0.4.0

# ── Computer vision / image ─────────────────────────────────────────────────
opencv-python-headless>=4.9.0
Pillow>=10.0.0
imageio>=2.28.0
imageio-ffmpeg>=0.4.9
scikit-image>=0.21.0

# ── Audio processing ────────────────────────────────────────────────────────
librosa>=0.10.0
soundfile>=0.12.1
resampy>=0.4.2
pydub>=0.25.1
av>=11.0.0

# ── Numerical / scientific ──────────────────────────────────────────────────
# numpy < 2.0 required by basicsr / older scipy APIs
numpy>=1.24.0,<2.0.0
scipy>=1.11.0

# ── ML utilities ────────────────────────────────────────────────────────────
einops>=0.7.0
kornia>=0.7.0

# ── Face detection / landmark ───────────────────────────────────────────────
face-alignment>=1.4.1
# dlib requires cmake; installed separately in Colab notebook

# ── 3D face fitting ─────────────────────────────────────────────────────────
# h5py not strictly required by SadTalker but kept for compatibility
h5py>=3.10.0

# ── Enhancement stack ───────────────────────────────────────────────────────
# basicsr must be installed before gfpgan / realesrgan
basicsr>=1.4.2
facexlib>=0.3.0
realesrgan>=0.3.0
gfpgan>=1.3.8

# ── SadTalker utilities ─────────────────────────────────────────────────────
yacs>=0.1.8
pyyaml>=6.0
tqdm>=4.66.0
requests>=2.31.0
joblib>=1.3.0

# ── Notebook extras ─────────────────────────────────────────────────────────
ipython>=8.0.0
