# TalkForge — Python dependencies
# Install with:  pip install -r requirements.txt

# ── Web UI ─────────────────────────────────────────────────────────────────
gradio>=4.26.0,<5.0.0

# ── Deep learning ──────────────────────────────────────────────────────────
# torch / torchvision are installed by the Colab notebook via the CUDA wheel;
# list them here for local installs (CPU-only fallback).
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# ── Computer vision / image processing ─────────────────────────────────────
opencv-python-headless>=4.9.0
Pillow>=10.2.0
imageio>=2.34.0
imageio-ffmpeg>=0.4.9
scikit-image>=0.22.0

# ── Audio processing ────────────────────────────────────────────────────────
librosa>=0.10.1
soundfile>=0.12.1
resampy>=0.4.3

# ── Numerical / scientific ──────────────────────────────────────────────────
numpy>=1.24.0,<2.0.0
scipy>=1.12.0
einops>=0.7.0
kornia>=0.7.2

# ── Face detection & landmark ───────────────────────────────────────────────
# Required by SadTalker
face-alignment>=1.4.1
dlib>=19.24.2          ; platform_system != "Darwin" or platform_machine == "x86_64"

# ── 3D morphable model ──────────────────────────────────────────────────────
# Required by SadTalker's BFM fitting
h5py>=3.10.0

# ── Enhancement (GFPGAN / Real-ESRGAN) ─────────────────────────────────────
gfpgan>=1.3.8
realesrgan>=0.3.0
basicsr>=1.4.2

# ── Utilities ──────────────────────────────────────────────────────────────
tqdm>=4.66.2
pyyaml>=6.0.1
requests>=2.31.0
yacs>=0.1.8
addict>=2.4.0

# ── Colab / notebook extras (harmless to install locally) ──────────────────
ipython>=8.0.0
